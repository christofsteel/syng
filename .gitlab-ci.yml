code_quality:
    image: python:3
    script:
        - pip install mypy --quiet
        - pip install pylint --quiet
        - pip install . --quiet
        - mypy syng --strict
        - pylint syng
