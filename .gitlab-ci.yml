code_qualityt:
    image: python:3
    script:
        - pip install mypy --quiet
        - pip install pylint --quiet
        - pip install -e . --quiet        
        - mypy -p syng --strict
        - ls -al
        - pwd
        - pylint syng
